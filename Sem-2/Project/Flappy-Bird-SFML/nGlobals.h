#pragma once

constexpr int WIN_WIDTH = 600, WIN_HEIGHT = 768; // making constant integers of window size of the game
constexpr float SCALE_FACTOR = 1.5f; // to scale the images accordingly to the game's window size