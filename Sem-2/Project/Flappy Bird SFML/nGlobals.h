// This is the header file for the nGlobals namespace which contains the global variables and constants.
#pragma once

constexpr int WIN_WIDTH = 600, WIN_HEIGHT = 768; // The window's width and height
constexpr float SCALE_FACTOR = 1.5f; // The scale factor for the sprites
